import styles from './DashboardPage.module.scss';


import { imageGod } from '@/shared/assets/images/index.ts';

import { TaskCard } from '@/enteries/task-card/ui/index.ts';

export const DashboardPage = () => {
  return (
    <section className={`${styles.dashboard} page`}>

    </section>
  );
};